{
  "name": "vibe-notion",
  "version": "1.3.1",
  "description": "Notion API CLI for AI agents",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/devxoul/vibe-notion.git"
  },
  "bin": {
    "vibe-notion": "./src/platforms/notion/cli.ts",
    "vibe-notionbot": "./src/platforms/notionbot/cli.ts"
  },
  "type": "module",
  "main": "dist/cli.js",
  "module": "dist/cli.js",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc && tsc-alias --resolve-full-paths",
    "postbuild": "bun scripts/postbuild.ts",
    "test": "bun scripts/test.ts",
    "test:e2e": "bun test --config /dev/null e2e/",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxfmt --write .",
    "prepublishOnly": "bun run build && bun scripts/prepublish.ts",
    "postpublish": "git checkout package.json"
  },
  "dependencies": {
    "@notionhq/client": "latest",
    "@tryfabric/martian": "^1.2.4",
    "better-sqlite3": "latest",
    "commander": "latest",
    "remark-gfm": "^4.0.1",
    "remark-parse": "^11.0.0",
    "unified": "^11.0.5"
  },
  "devDependencies": {
    "@types/better-sqlite3": "latest",
    "@types/bun": "latest",
    "@types/node": "latest",
    "oxfmt": "latest",
    "oxlint": "latest",
    "tsc-alias": "^1.8.16",
    "typescript": "latest"
  }
}
